version: '3.7'

services:
  web:
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/football-matches-app
    ports:
      - "3000:3000"
    
 